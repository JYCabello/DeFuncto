# Unit
How many possible values can this function return?
```cs
public bool IsActive() => //...
```

Easy, isn't it?

What about this one?

```cs
public enum Bool
{
    True, 
    False, 
    FileNotFound
}
